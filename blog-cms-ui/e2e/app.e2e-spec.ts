import { BlogCmsUiPage } from './app.po';

describe('blog-cms-ui App', () => {
  let page: BlogCmsUiPage;

  beforeEach(() => {
    page = new BlogCmsUiPage();
  });

  it('should display message saying app works', () => {
    // page.navigateTo();
    // expect(page.getParagraphText()).toEqual('app works!');
  });
});
